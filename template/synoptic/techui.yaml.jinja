beamline:
  short_dom: "{{ domain | upper }}"
  long_dom: "{{ location | upper }}"
  desc: "{{ domain | upper }} Beamline or Tech Area"
  # placeholder: the tech-ui pre-commit hook rewrites https://xxx-opis/ in
  # the generated screens to ../, so they carry no host name
  url: xxx-opis

# add components for techUI-builder to build screens for
components: {}

  # DCAM1:
  #   desc: GigE camera EXAMPLE
  #   prefix: {{ location | upper }}-DI-DCAM-01
